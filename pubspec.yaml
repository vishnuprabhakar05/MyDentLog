name: my_dentlog_app
description: "My Dentlog App"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.12.1
  firebase_database: ^11.3.4
  url_launcher: ^6.3.1
  google_sign_in: ^6.2.2  # 🔹 Required for Google Authentication
  googleapis: ^12.0.0  # 🔹 Google APIs, including Drive
  googleapis_auth: ^1.4.0  # 🔹 Authentication for Google APIs
  http: ^1.2.0  # 🔹 Required for network requests
  file_picker: ^9.2.1
  firebase_auth: ^5.5.1
  provider: ^6.1.2
  uuid: ^4.5.1
  image_picker: ^1.1.2
  get: ^4.7.2
  extension_google_sign_in_as_googleapis_auth: ^2.0.0
  path_provider: ^2.0.14
  mime: ^1.0.4




dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/lab_work_type.json  
